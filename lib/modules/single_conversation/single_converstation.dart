import 'package:flutter/material.dart';

class SingleConversation extends StatelessWidget {
  const SingleConversation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
